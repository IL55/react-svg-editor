'use strict';

describe('ChangeTestActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/ChangeTestActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});

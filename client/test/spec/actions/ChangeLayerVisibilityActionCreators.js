'use strict';

describe('ChangeLayerVisibilityActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/ChangeLayerVisibilityActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
